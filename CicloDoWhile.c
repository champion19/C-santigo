#include <stdio.h>
int main()
{
  int contador=1;
  do{
  printf("%i",contador);//imprime los valores del 1 al 10,este bucle solo se ejecuta una vez, si no se cumple la oondicion,entonces termina el programa
  contador ++;
  }while (contador <=10);
    return 0;
}
